PORT=3636
DATABASE_URL=your-database_url
MOCK_DATABASE_URL=your-mock_database_url
GOOGLE_CLIENT_ID=your-google_client_id
JWT_SECRET=your-jwt-secret

# Frontend URL (used in password reset email link)
FRONTEND_URL=http://localhost:5173

# SMTP (optional - leave blank to use console.log for reset links in dev mode)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
